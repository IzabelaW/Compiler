VAR
    a b c d e f g h i j
BEGIN
    a := 3;
    b := 5;

    IF a < b THEN
        WRITE 1;
    ENDIF

    IF a < b THEN
        WRITE 1;
    ELSE
        WRITE 0;
    ENDIF

    IF a < 2 THEN
        WRITE 1;
    ENDIF

    IF a < 2 THEN
        WRITE 1;
    ELSE
        WRITE 0;
    ENDIF

    IF 2 < b THEN
        WRITE 1;
    ENDIF

    IF 2 < b THEN
        WRITE 1;
    ELSE
        WRITE 0;
    ENDIF

    IF 2 < 3 THEN
        WRITE 1;
    ENDIF

    IF 2 < 3 THEN
        WRITE 1;
    ELSE
        WRITE 0;
    ENDIF
END
